[project]
name = "tweet-me"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "PyGithub>=2.1",
    "openai>=1.30",
    "python-telegram-bot>=21.0",
    "tweepy>=4.14",
    "python-dotenv>=1.0",
]

[project.optional-dependencies]
dev = ["ruff"]
